# Contracts: Runtime Folder & Namespace Reorganization

No new external interface contracts are introduced by this feature.

This is a pure structural refactor — all type names, method signatures, and public contracts
remain identical. Only namespace paths change, which affects `using` statements but not the
public API surface (OSR-003).

